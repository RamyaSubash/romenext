package com.els.romenext.core.enums;

/**
 * This enumeration is used to denote the Rome class that can be be represented as a node.
 * Current we have RomeType and RomeRule in the system 
 * @author jhsiao
 *
 */
public enum RomeNodeClassEnum {
	TYPE, RULE, CONNECTION
};